# Frontend (Vite) configuration. Copy to ".env" to override the defaults.
# Node/Express API gateway (auth + patient records):
VITE_API_BASE=http://localhost:5005
# Python ML/inference service (prediction, Grad-CAM, biomarker report, chatbot):
VITE_ML_BASE=http://localhost:5001
